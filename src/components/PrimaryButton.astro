---
interface Props {
    text: string,
    class?:string,
    link:string
}
const {text, link} = Astro.props;
---
<a href=`${link}` class=`bg-gradient-to-r from-[#0A767B] to-[#00A7D6] w-max py-4 px-10 rounded-lg text-base transition-transform hover:translate-y-1 ${Astro.props.class}`>{text}</a>